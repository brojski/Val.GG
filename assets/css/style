/* Adjust layout for screens smaller than 992px */
@media (max-width: 992px) {
  .video-container {
    display: flex;
    flex-direction: column; /* Stack elements vertically */
    align-items: center;
    justify-content: center;
    gap: 20px;
    margin: 0 auto;
    max-width: 100%;
    padding: 20px;
  }

  .video-buttons {
    display: flex; /* Use flexbox for horizontal alignment */
    flex-wrap: wrap; /* Allow wrapping if needed */
    justify-content: center; /* Center-align buttons */
    gap: 40px; /* Add spacing between buttons */
    margin-bottom: 20px;
    order: -1; /* Ensure buttons are above the video and description */
  }

  .video-buttons img {
    filter: invert(100%); /* Invert colors for better visibility */
    width: 50px; /* Adjust button size */
    height: auto;
    margin: 0; /* Remove unnecessary margins */
    display: inline-block; /* Keep buttons inline */
  }

  /* .video-display {
    width: 100%;
    text-align: center;
  }

  .video-display video {
    width: 100%;
    max-width: 600px;
    height: auto;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
  } */

  .video-description {
    width: 100%;
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
    font-size: 14px;
  }

  .video-description h3 {
    font-size: 18px;
  }

  .video-description p {
    font-size: 13px;
  }
}

/* Adjust layout for screens smaller than 768px */
@media (max-width: 768px) {
  .video-container {
    gap: 15px;
    padding: 10px;
  }

  .video-buttons img {
    filter: invert(100%);
    width: 40px; /* Further reduce button size */
    margin: 5px;
  }

  .video-description h3 {
    font-size: 16px;
  }

  .video-description p {
    font-size: 12px;
  }
}